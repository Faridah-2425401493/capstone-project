version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
      # Prevent local vendor folder from overwriting the container's vendor folder
      - /var/www/html/vendor
